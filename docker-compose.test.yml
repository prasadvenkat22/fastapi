version: '3.8'
services:
  mongo:
    image: mongo:6.0
    container_name: fastapi_test_mongo
    ports:
      - "27017:27017"
    healthcheck:
      test: ["CMD", "bash", "-lc", "mongo --eval 'db.runCommand({ ping: 1 })' --quiet || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
